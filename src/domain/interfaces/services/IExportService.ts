/**
 * @fileoverview Interface for export services.
 * Defines the contract for data export functionality.
 *
 * @author TripFood Manager Team
 * @version 1.0.0
 */

/**
 * Interface for export services.
 * Implemented by infrastructure services that handle data export.
 *
 * @description
 * This interface defines the contract for export functionality.
 * Implementation details (CSV, Excel, PDF, etc.) are left to
 * the infrastructure layer.
 */
export interface IExportService {
  // This is a marker interface.
  // The actual implementation methods are specific to each export format
  // and defined in the infrastructure layer.
}
